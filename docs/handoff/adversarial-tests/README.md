# Adversarial Safety Test Catalog

`catalog.yaml` is the initial machine-readable index.

The implementation should progressively turn these cases into executable Go integration/architecture tests.

Every newly discovered material safety failure should become a regression case where technically feasible.
